export enum NotificationStatus {
  ACTIVE = 'active',
  SENT = 'sent',
  ARCHIVE = 'archive',
}

export enum NotificationTags {
  CUSTOM = 'custom',
  ALERT = 'alert',
  SOCIAL = 'social',
  EVENT = 'event',
  SYSTEM = 'sytem',
}

export enum NotificationMedium {
  EMAIL = 'email',
  WHATSAPP = 'whatsapp',
  IN_APP = 'in-app',
}
